name: paperdoll
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.13.0
resolution: workspace

dependencies:
  device_info_plus: ^13.2.0
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  flutter_secure_storage: ^10.3.1
  freezed_annotation: ^3.1.0
  go_router: ^18.0.0
  logging: ^1.3.0
  material_ui: ^1.0.0
  openapi:
    path: ../api/dart_binding
  package_info_plus: ^10.2.1
  riverpod_annotation: ^4.0.0
  url_launcher: ^6.3.2
  webview_flutter: ^4.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  riverpod_generator: ^4.0.0

  freezed: ^4.0.0-dev.3
  patrol: ^4.9.0
  http: ^1.6.0
  collection: ^1.19.0
  patrol_finders: ^3.6.0
  # Faking the WebView platform in reader widget tests (see
  # test/src/fake_webview.dart).
  webview_flutter_platform_interface: ^2.15.1
  plugin_platform_interface: ^2.1.8

flutter:
  uses-material-design: true

patrol:
  app_name: Test
  test_directory: e2e
  android:
    package_name: dev.norelease.paperdoll
